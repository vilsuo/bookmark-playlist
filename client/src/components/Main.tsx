import VideoContainer from './video/VideoContainer';
import QueueTable from './queue/QueueTable';

const Main = () => {
  return (
    <div className="main">
      <VideoContainer />

      <QueueTable />
    </div>
  );
};

export default Main;
