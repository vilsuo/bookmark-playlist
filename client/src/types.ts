export type Album = {
  videoId: string;
  artist: string;
  title: string;
  published: number;
};

export type Link = {
  text: string;
  href: string;
};

export type Position = {
  x: number,
  y: number;
};
